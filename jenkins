pipeline {
    agent any
    stages {
        stage('Git-pull') {
            steps {
                git branch: 'main', url: 'https://github.com/shubhamkalsait/EasyCRUD.git'
            }
        }
        stage('Build') {
            steps {
               sh '''cd backend
                    mvn clean package -DskipTests'''
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deoply Success'
            }
        }
    }
}
